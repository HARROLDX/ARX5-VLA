包路径：/home/agilex/kai0/train_deploy_alignment/dagger/arx

# 4d世界模型-真机测试

# 绝对 EE pose--wm predict scene flow 轨迹部署
conda activate kai0_inference
source /opt/ros/humble/setup.bash
source /home/agilex/ARX_X5-main/ARX_X5-main/ROS2/X5_ws/install/setup.bash
cd /home/agilex/kai0/train_deploy_alignment/dagger/arx


python3 /home/agilex/kai0/train_deploy_alignment/dagger/arx/arx_replay_ee_pose_hdf5_left_single.py \
  --hdf5_path /home/agilex/kai0/kai0_data/ARX_data_test1/green_tray/ours_demo/tra_1.hdf5 \
  --status_topic /arm_status \
  --command_topic /arm_cmd \
  --pose_dataset observations/ee_pose \
  --gripper_dataset observations/gripper \
  --robot_cmd_mode 4 \
  --goto_first \
  --speed 0.8

含义是：先用 IK 平滑移动到第一帧 EE pose，再按 HDF5 里的绝对 ee_pose 发 /arm_cmd.end_pos。

注意：这个 replay 必须配 open_single_arm.launch.py，不能配 remote slave 的 /arm_slave_l_status / /arm_master_l_status 那套。



# 该脚本可以把遥操作关节数据转化为eepose
/home/agilex/kai0/train_deploy_alignment/dagger/arx/convert_arx_joint_hdf5_to_ee_pose_arx.py




# 单左臂案例-数据采集

使用方法：
按空格开始录制，按s保存一条；然后再按空格开始录制下一条。如果想放弃刚刚的数据，则使用n即可清空上一条


采集--先启动主从
此脚本包含回到零点的功能，因此注意采集数据后，机械臂尽量靠近一些零点
此脚本默认多一个第三视角的realsense相机采集Video以及深度图
也就是他可以保存high，wirst和third
但注意，third和high的深度采集只能开一个！
记得先启动机械臂 source

conda activate kai0_inference
source /opt/ros/humble/setup.bash
source /home/agilex/ARX_X5-main/ARX_X5-main/ROS2/X5_ws/install/setup.bash
cd /home/agilex/kai0/train_deploy_alignment/dagger/arx

python arx_teleop_collect_left_single_aligned.py \
  --dataset_dir /home/agilex/kai0/kai0_data/ARX_data_test1 \
  --dataset_name grasp_cup \
  --camera_front_backend orbbec \
  --enable_camera_high true \
  --enable_camera_left_wrist false \
  --enable_camera_third false \
  --save_high_depth true \
  --save_third_depth false


数据集回放
python arx_replay_hdf5_left_single.py \
  --hdf5_path /home/agilex/kai0/kai0_data/arx_teleop/task_1_left_single_collect_001/episode_3.hdf5 \
 --dataset observations/qpos \
  --control_frequency 30 \
  --speed 0.6














# ARX 双臂纯遥操作数据采集
代码包括两个版本
第一个版本为arx_teleop_collect.py。他不会去和iference一样先对齐位置
第二个版本为arx_teleop_collect_aligned.py，结束一条录制时，他会回到0位，然后再按空格，下一条



# 数据replay
python train_deploy_alignment/dagger/arx/arx_replay_hdf5.py \
  --hdf5_path /home/agilex/kai0/kai0_data/arx_teleop/task_1_collect_001/episode_12.hdf5 \
  --dataset observations/qpos \
  --control_frequency 30 \
  --speed 0.6
  若speed为0.6,则还有略微发抖的现象


## 1. 启动机械臂 ROS2 主从节点

数据采集脚本不会启动或停止机械臂节点。先在一个终端启动 ARX master/slave：

```bash
cd /home/agilex/ARX_X5-main/ARX_X5-main/ROS2/X5_ws
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch arx_x5_controller open_remote_master.launch.py
ros2 launch arx_x5_controller open_remote_slave.launch.py
```

保持这个终端运行。后面即使采集脚本按 `q` 退出，机械臂 master/slave 节点也会继续运行。

## 2. 打开采集终端

另开一个终端：

```bash
conda activate kai0_inference
source /opt/ros/humble/setup.bash
source /home/agilex/ARX_X5-main/ARX_X5-main/ROS2/X5_ws/install/setup.bash
cd /home/agilex/kai0/train_deploy_alignment/dagger/arx
当前应使用 `kai0_inference` 环境。
```

## 3. RGB + 深度采集
第三视角使用 Orbbec backend：

```bash


对齐
python arx_teleop_collect_aligned.py \
  --dataset_dir /home/agilex/kai0/kai0_data/arx_teleop \
  --dataset_name task_1_collect_001 \
  --camera_front_backend orbbec \
  --save_depth false \
  --align_depth_to_color true \
  --camera_left_serial 913522070540 \
  --camera_right_serial 246422072014

不需要对齐
python arx_teleop_collect.py \
  --dataset_dir /home/agilex/kai0/kai0_data/arx_teleop \
  --dataset_name task_1_collect_001 \
  --camera_front_backend orbbec \
  --save_depth false \
  --align_depth_to_color true \
  --camera_left_serial 913522070540 \
  --camera_right_serial 246422072014

```

说明：
- `--save_depth false` 是默认值。
- 只有 `--camera_front_backend orbbec --save_depth true` 才会写深度图。
- `--align_depth_to_color true` 会把深度尽量对齐到第三视角 RGB 图像坐标系。
- DaBai DC1 不支持 frame sync，运行时可能出现 OrbbecSDK warning，例如 `pFrame is nullptr` 或 `Current device does not support frame sync`。如果采集帧数持续增长、深度 PNG 正常写出，一般不是致命问题。

帧率：奥比中光30hz，主循环control采集频率30hz


## 4. 键盘控制数据记录

脚本启动后默认暂停，按键如下：

```text
Space  开始 / 暂停采集
s      保存当前 episode
n      丢弃当前 episode，清空缓存并继续使用当前 episode 编号
q      退出采集脚本，ARX master/slave 节点继续运行
```

保存时会暂停采集，后台写 HDF5、mp4、深度 PNG，然后进入下一个 episode。

对于 `arx_cam_high_collect.py`，按键保持一致，但保存时只会写：
- `cam_high` 的 mp4
- `cam_high` 的深度 PNG
- `cam_high` 的 `_meta.json`

## 5. 保存结构

一次采集会保存到：

```
/home/agilex/kai0/kai0_data/arx_teleop/task_1_collect_001/
  episode_0.hdf5
  video/cam_high/0.mp4
  video/cam_left_wrist/0.mp4
  video/cam_right_wrist/0.mp4
```

如果开启深度，还会多出：

```text
/home/agilex/kai0/kai0_data/arx_teleop/task_1_collect_001/
  depth/cam_high/0/000000.png
  depth/cam_high/0/000001.png
  depth/cam_high/0_meta.json
```
深度 PNG 是 16-bit PNG，单位是毫米。普通图片查看器打开可能是一片黑，这是正常的，因为它不是给人直接看的彩色图，而是保留真实深度值的数据文件。


## 6. 深度 meta 内容

`depth/cam_high/<episode>_meta.json` 会记录：

```
episode
camera
format
unit
depth_width / depth_height
depth_scale
color_width / color_height
aligned_to_color
align_mode
camera_param
```

其中 `camera_param` 来自 OrbbecSDK 读取到的相机出厂标定：

```text
camera_param.depth_intrinsic
camera_param.rgb_intrinsic
camera_param.depth_distortion
camera_param.rgb_distortion
camera_param.depth_to_rgb_transform
```
注意：这里的 `depth_to_rgb_transform` 是 Orbbec 相机内部深度和彩色传感器之间的变换，不是相机到机械臂 base 的外参。后续如果要把点云转到机器人坐标系，还需要单独标定 `T_base_camera`。



## 7. 深度图彩色预览和 RGB 点云

后处理预览脚本：
把传入的depth文件下的camera third/high的第一帧和最后一帧深度图转成彩色预览图，并生成 RGB 点云：

```bash

python train_deploy_alignment/dagger/arx/visualize_depth_pointcloud.py \
  --depth_root /home/agilex/kai0/kai0_data/arx_teleop/task_1_left_single_collect_001/depth \
  --frames first,last \
  --show false

ply-rgb显示颜色: meshlab 右下角dot decorator点击他从而显示颜色

## 8. Orbbec SDK 环境

当前机器已经编译并安装了 DaBai DC1 可用的 OrbbecSDK v1 Python wrapper：

```bash
/home/agilex/kai0/third_party/pyorbbecsdk_v1
```

如果以后重装环境，可按下面方式重新编译安装：

```bash
cd /home/agilex/kai0/third_party/pyorbbecsdk_v1
conda activate kai0_inference
python -m pip install pybind11 pybind11-global

mkdir -p build
cd build
cmake -Dpybind11_DIR=$(pybind11-config --cmakedir) ..
make -j$(nproc)
make install

cd ..
python setup.py bdist_wheel
python -m pip install --force-reinstall dist/pyorbbecsdk-*.whl
```

如果普通用户没有相机 USB 权限，安装 udev 规则后重新插拔相机：

```bash
cd /home/agilex/kai0/third_party/pyorbbecsdk_v1
sudo bash scripts/install_udev_rules.sh
sudo udevadm control --reload-rules
sudo udevadm trigger
```

验证 OrbbecSDK 是否能看到 DaBai DC1：

```bash
conda activate kai0_inference
python -c "from pyorbbecsdk import Context; ctx=Context(); dl=ctx.query_devices(); print(dl.get_count())"
```

正常应能看到 `1` 台设备。




### 点云要转到机器人坐标系

当前 meta 只保存相机内部参数。要把点云从相机坐标系转到 ARX base 或世界坐标系，需要后续标定第三视角相机外参，例如：

```text
T_base_cam_high
```

这部分暂时未加入采集流程。




## dagger代码没问题


# realsense
这个命令测试当前电脑连接的所有 RealSense D435i以及奥比中光
cd /home/agilex/kai0/train_deploy_alignment/inference/arx/inference
/home/agilex/miniconda3/envs/kai0_inference/bin/python detect_realsense_devices.py \
  --visualize \
  --color-width 640 \
  --color-height 480 \
  --fps 30

new third view
[0]
  name          : Intel RealSense D435
  serial        : 836612070632








# yuhai世界模型数据采集流程

# 采集
conda activate kai0_inference
source /opt/ros/humble/setup.bash
source /home/agilex/ARX_X5-main/ARX_X5-main/ROS2/X5_ws/install/setup.bash
cd /home/agilex/kai0/train_deploy_alignment/dagger/arx

  
  python arx_teleop_collect_yuhai_left_single.py \
  --dataset_dir /home/agilex/kai0/kai0_data/arx_teleop_yuhai \
  --dataset_name task_1_yuhai_left_single_collect_001 \
  --action_topic /arm_master_l_status \
  --action_msg_type robot_status
所有数据都存在hdf5


# 回放
ik模式的launch不同
启动 open_single_arm.launch.py

conda activate kai0_inference
source /opt/ros/humble/setup.bash
source /home/agilex/ARX_X5-main/ARX_X5-main/ROS2/X5_ws/install/setup.bash
cd /home/agilex/kai0/train_deploy_alignment/dagger/arx

python arx_replay_yuhai_left_single.py \
  --hdf5_path /home/agilex/kai0/kai0_data/arx_teleop_yuhai/task_1_yuhai_left_single_collect_001/episode_000000.hdf5 \
  --status_topic /arm_status \
  --command_topic /arm_cmd \
  --command_msg_type robot_cmd \
  --speed 0.7


# 数据集上传至huggingface
Hugging Face 创建一个 access token




