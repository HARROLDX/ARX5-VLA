## 数据采集脚本
/mnt/workspace/xiajiawei/kai0/train_deploy_alignment/dagger/arx/arx_teleop_collect_aligned.py
节点通过“话题名 + 消息类型”来订阅或发布数据
topic 名 = 频道名
message type = 频道里每条消息的格式
publisher = 发消息的人
subscriber = 收消息的人

当前节点订阅 /arm_slave_l_status 这个 topic
要求这个 topic 上传输的数据类型是 RobotStatus
收到消息后调用 joint_left_callback(msg)

它依赖 ARX X5 的 ROS2 驱动/控制节点已经在运行，并通过 ROS2 topic 和 service 与驱动配合


ARX X5 ROS2 驱动节点
        ↓ 发布 RobotStatus
/arm_slave_l_status
/arm_slave_r_status
/arm_master_l_status
/arm_master_r_status
        ↓
ARXTeleopObserver 订阅
        ↓
同步读取三路相机
cam_high / cam_right_wrist / cam_left_wrist
        ↓
build_observation()
        ↓
select_action()
        ↓
TeleopCollector.add_frame()
        ↓
episode_x.hdf5 + video/*.mp4 + 可选 depth/*.png









## 推理脚本
/mnt/workspace/xiajiawei/kai0/train_deploy_alignment/inference/arx/inference/arx_openpi_inference_temporal_smooth.py

ARX-X5 双臂推理部署脚本
读取机械臂当前状态 + 三路相机图像
        ↓
通过websocket把payload发给远端策略模型 openpi server
        ↓
模型返回一段未来动作 chunk
        ↓
，actions chunk 进入 StreamActionBuffer，上本地用 temporal smoothing 平滑动作
        ↓
再发布到ros2话题，按 30Hz 一步一步发布给 ARX X5 驱动
模型服务端有归一化和反归一化
模型不直接控制电机；模型只输出 14 维目标关节位置序列，脚本再通过 ROS2 RobotStatus.joint_pos 把这些目标位置发布给 ARX X5 的控制节点，由 ARX 驱动真正执行。



## 











