# 配合点流工具包的运行
/mnt/workspace/xiajiawei/kai0/train_deploy_alignment/dagger/arx/arx_replay_ee_pose_hdf5_left_single.py




# 真机部署前验证方式
启动server
cd /mnt/workspace/xiajiawei/kai0
三相机推理
checkpoints/dp_arx-left-3cam
现在正确，之前不正确因为model_dp/configuration_dp.py训练端和部署端没有同步
~/kai0/run_rtc.sh \
  --host 127.0.0.1 \
  --port 8000 \
  --visualize_cameras true \
  --camera_front_color rgb \
  --camera_left_color bgr \
  --debug_actions \
  --debug_action_interval 1 \
  --dry_run_no_publish

三相机的参数：
  --include_third_camera true \
  --camera_third_backend realsense \
  --camera_third_serial 836612070632 \
  --camera_third_color bgr \



# 整体处理流程

根据情况需要，选择在xjw1或者xjw3进行训练
本地ipc负责数据采集和推理，部署



# 单臂左臂实验diffusion全流程
可选wrist，high，third三路相机
但经过测试，实际上，wrist+high的两路相机更好一些
dp_arx_left_3cam是三相机版本
dp_arx_left是双相机，他们都是单左臂






# xjw1有网服务器训练：
uv 可用：uv 0.11.0
项目 .venv 存在：/mnt/workspace/xiajiawei/kai0/.venv





# xjw3离线服务器训练：
# 数据上传至xjw3
将数据传到xjw3服务器上--port 7832
scp -r /home/agilex/kai0/kai0_data/arx_teleop xjw3:/mnt/workspace/wudong/arx_data
输入yes


# 在 xjw3 上转换 lerobot 数据：
cd /mnt/workspace/xiajiawei/kai0
export LD_LIBRARY_PATH=/mnt/workspace/xiajiawei/ffmpeg61_libs:${LD_LIBRARY_PATH:-}
export HF_HUB_OFFLINE=1
export HF_DATASETS_OFFLINE=1
bash scripts/convert_to_lerobot_left_single.sh \
  --source-dir /mnt/workspace/xiajiawei/kai0/data/task_1_left_single_collect_001 \
  --output-dir /mnt/workspace/xiajiawei/kai0/data/task_1_left_single_collect_dp_001 \
  --task-name "grasp the cup and place it on the black plate"


数据归一化文件生成
uv run --offline scripts/compute_norm_states_fast.py \
  --config-name dp_arx_left \
  --action-dim 7


训练
若开启tmux，则需要先：
cd /mnt/workspace/xiajiawei/kai0
export PATH=/mnt/workspace/xiajiawei/bin:$PATH
export UV_CACHE_DIR=/mnt/workspace/xiajiawei/uv_cache
export UV_PYTHON_INSTALL_DIR=/mnt/workspace/xiajiawei/uv_python
export UV_LINK_MODE=copy
export UV_NO_PROGRESS=1
export LD_LIBRARY_PATH=/mnt/workspace/xiajiawei/ffmpeg61_libs:${LD_LIBRARY_PATH:-}
export HF_HUB_OFFLINE=1
export HF_DATASETS_OFFLINE=1

uv run --offline scripts/train_pytorch.py dp_arx_left \
  --exp-name=dp_left_2cam_run_001 \
  --checkpoint-base-dir=/mnt/workspace/xiajiawei/kai0/checkpoints \
  --batch-size=32 \
  --num-workers=8 \
  --num-train-steps=20000 \
  --save-interval=10000 \
  --log-interval=20 \
  --overwrite \
  --no-wandb-enabled \
  2>&1 | tee logs/dp_arx_left_2cam_run_001.log


推理
uv run --offline scripts/serve_policy.py policy:checkpoint \
  --policy.config=dp_arx_left \
  --policy.dir /home/agilex/kai0/checkpoints/dp_arx_left



客户端真机部署：
保持和如下一致，但run sh里的配置文件一定记得修改，切换run sh里的内容
默认情况下的dp就是双相机，关键在于include_third_camera true
conda activate kai0_inference
~/kai0/run_rtc.sh \
  --host 127.0.0.1 \
  --port 8000 \
  --visualize_cameras true \
  --include_third_camera true \
  --camera_third_backend realsense \
  --camera_third_serial 836612070632 \
  --camera_front_color rgb \
  --camera_left_color bgr \
  --camera_third_color bgr \
  --latency_k 1 \
  --min_smooth_steps 31 \
  --max_joint_delta_per_step 0.08
  





# 切记推理脚本里，指定位置的设置
arx_openpi_inference_temporal_smooth.py的第458行-归零的位置
部署时，一定注意切换run sh里的模式



# log图
用export WANDB_API_KEY='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'
这个方法可以解决wanb的api key更新的问题，但是服务器没网

最终采取上述方法，将log文件本地保存，然后本地使用脚本画图
cd /mnt/workspace/xiajiawei/kai0
uv run --offline scripts/plot_training_metrics.py logs/dp_arx_left_3cam_10000.log --metrics loss grad_norm param_norm

三幅图的含义
loss（训练损失）：compute_loss(...) 在一个 batch 上的平均值，即 train_step里反向传播的目标函数
grad_norm（梯度范数）：本 step 里所有 可训练参数 的梯度拼起来后的全局 L2 范数 optax.global_norm(grads)，见 train.py:184。它衡量"这一步要往参数上推多大力"。
param_norm（参数范数）：所有"是 kernel 的"参数（排除 bias / scale / pos_embedding / input_embedding，且 ndim>1 的权重）拼起来的全局 L2 范数，它反映 整个模型权重的整体大小。






# 下载checkpoint到本地
如果文件太大，可以先把checkpoint传到xjw1再拉到本地；xjw3是无网训练服务器，xjw1可作为中转站。

scp -P 7832 -r root@116.198.70.4:/mnt/workspace/xiajiawei/kai0/checkpoints/dp_arx_left/dp_left_2cam_run_001/20000 /home/agilex/kai0/checkpoints/dp_arx-left

rsync -avP --partial --append --inplace -e "ssh -p 114" root@116.198.71.58:/mnt/workspace/xiajiawei/kai0/checkpoints/arx5_finetune  /home/agilex/kai0/checkpoints










# 本地ipc运行推理kai0-vla
exec bash

终端0
首先，单独一个终端
启动机械臂本身，slave单独启动


ARX-TASK C：
终端1
conda activate kai0_inference
uv run --offline scripts/serve_policy.py policy:checkpoint \
  --policy.config=pi05_hang_cloth_normal \
  --policy.dir=/home/agilex/kai0/checkpoints/Task_C/mixed_5


微调后的模型推理，格式和taskc保持一致即可
# 由此，可以推断，机械臂本体，数据采集，taskc的训练和部署均无问题
conda activate kai0_inference
uv run --offline scripts/serve_policy.py policy:checkpoint \
  --policy.config=pi05_hang_cloth_normal \
  --policy.dir=/home/agilex/kai0/checkpoints/arx5_finetune1/pi05_hang_cloth_normal/arx5_collect001_taskc_hang_4000/3999



终端2
conda activate kai0_inference
~/kai0/run_rtc.sh \
  --host 127.0.0.1 \
  --port 8000 \
  --visualize_cameras true

本机 server + --host 127.0.0.1：本地推理则网络不影响推理通信延迟。
train_deploy_alignment/inference/arx/inference/arx_openpi_inference_temporal_smooth.py
暂停时，一定要选择暂停终端2，run_rtc.sh，机械臂会在原地不动，然后再运行一次run_rtc.sh回到零位置
run_rtc.sh内部的脚本需要和serve_policy.py里的policy.config相匹配


服务器：
98a0d28b471ef45c2cba5da711046b2b
Host xjw1
    HostName 116.198.71.58
    User root
    Port 114

从服务器下载checkpoint
rsync -a --info=progress2 --partial --append-verify \
  -e "ssh -p 114 -o StrictHostKeyChecking=accept-new" \
  root@116.198.71.58:/mnt/workspace/xiajiawei/kai0/checkpoints/Task_C \
  /home/agilex/kai0/checkpoints/

目前这个中间相机是奥比中光dabai2436，https://github.com/orbbec/OrbbecSDK








# Kai0-VLA模型训练全流程（重要！）
# 训练概论
→ 生成一份“原始采集数据”：每个 episode 一个 HDF5，外加三路相机 MP4，可选 intervention JSON
→ 用 convert_h5_lerobot.py 转成 LeRobot 数据集
→ 跑 compute_norm_states_fast.py 生成 norm_stats.json
→ 跑 scripts/train.py，taskc-基于 config.py 里的 pi05_hang_cloth_normal 训练
→ 在 ./checkpoints/<config>/<exp_name>/... 下面得到训练产物
→ GPU 机上 scripts/serve_policy.py 起服务
→ ARX IPC/工控机上运行 arx_openpi_inference_rtc.py（或同目录其他推理脚本，rtc，非rtc，时间平滑等，必须和policy config匹配）做真机验证

# 数据转换运行
uv run --offline train_deploy_alignment/data_augment/convert_h5_lerobot.py \
  --data-dir /home/agilex/kai0/kai0_data/arx_teleop \
  --save-dir /home/agilex/kai0/kai0_data/lerobot_out \
  --repo-ids task_1_collect_002 \
  --prompt "task 1 collect" \
  --save-repoid task_1_collect_002_lerobot \
  --overwrite

features.json 由1,14改为了单纯的14
同时，转换后的lerobot数据有一些略微不对的地方


# HDF5 + 三路 MP4 怎么变成 LeRobot
lerobot格式
HangCloth/
  base/
    data/chunk-000/episode_000000.parquet
    videos/chunk-000/observation.images.hand_left/episode_000000.mp4
    videos/chunk-000/observation.images.hand_right/episode_000000.mp4
    videos/chunk-000/observation.images.top_head/episode_000000.mp4
    meta/info.json
    meta/episodes.jsonl
    meta/tasks.jsonl
    meta/episodes_stats.jsonl


# 归一化数据-compute_norm_states_fast.py产出norm_stats.json
读 config.py 里这个 config 对应的数据路径--repo id
递归读取所有 parquet
抽取 observation.state 和 action
计算 mean / std / q01 / q99
写出 norm_stats.json
作为针对机器人本体的归一化和反归一化

# config.py 和训练代码是怎么配合工作的
src/openpi/training/config.py：定义所有命名配置
scripts/train.py：只负责按配置训练
scripts/serve_policy.py：按配置 + checkpoint 起推理服务
所有的超参数定义，训练（推理）策略都在config.py

scripts/train.py 的主干结构
训练入口基本就是：
解析 config name
初始化 checkpoint 目录
创建 data loader
初始化模型
用 weight_loader 加载 base checkpoint
进入 JAX 训练循环
定期保存 checkpoint。

# config中的配置举例
    TrainConfig(
        name="pi05_hang_cloth_normal", #对于config的命名，具体配置如下，主要涉及机械臂本体dataconfig，数据集路径，checpoint路径，超参数等
        model=pi0_config.Pi0Config(pi05=True),
        data = LerobotARXDataConfig(
            repo_id="<path_to_repo_root>/data/HangCloth/base",  #训练时是数据集绝对路径，推理时读取归一化json文件在哪
            default_prompt="Fetch and hang the cloth.",
            use_delta_joint_actions=False, #关键！awbc部分时，开启为true，从而覆盖默认prompt
        ),
        weight_loader=weight_loaders.CheckpointWeightLoader("<path/to/pi05_base/checkpoint>"), #训练时的base checkpoint 路径，推理时不需要，下面的超参数在推理时也不需要
        num_train_steps=100_000,
        keep_period=5000,
        num_workers=8,  #cpu并行读取数据的线程数
        batch_size=256,
    ),

# 其他超参数：
学习率调度（warmup-峰值-最小学习率（余弦退火））；adamw优化器；ema指数移动平均衰减率（EMA 会把最近几百步的模型状态做个平均）；参数冻结；训练精度：输入接口很多都是float32，训练参数是bf16，优化器参数是32

# 推理时读取文件的路径：
checkpoint路径: 在 serve_policy.py 的命令行参数 --policy.dir 指定（对应 Checkpoint.dir）
config名: --policy.config 指定，用于从 _CONFIGS_DICT 查找对应的 TrainConfig
asset_id: 从 TrainConfig.data 的配置中取得（如 LerobotARXDataConfig 中 repo_id 会作为默认 asset_id）
norm_stats路径: 由 checkpoint_dir/assets/ + asset_id 拼接而成（checkpoints.py 第110行）


推理时的初始化顺序
根据config名获取 TrainConfig
定位 checkpoint 目录
检测是PyTorch还是JAX模型
加载模型权重
通过 data.create() 构建数据变换pipeline
从 checkpoint 的 assets 目录加载 norm_stats
组装 Policy 对象（包含 input/output transforms + 模型）


Pi0Config(
    pi05=True,              # 使用 π0.5 架构（adaRMSNorm注入timestep + state作为离散token）
    action_dim=32,          # 模型内部动作维度（ARX实际14维，补0到32）
    action_horizon=50,      # 一次预测50步动作（action chunk size）
    max_token_len=200,      # prompt最大token长度（pi05默认200，pi0默认48）
    paligemma_variant="gemma_2b",      # 视觉语言骨干：2B参数PaliGemma
    action_expert_variant="gemma_300m", # 动作专家：300M参数Gemma
    discrete_state_input=True,          # state作为离散token输入（pi05特性）
)

LerobotARXDataConfig 主要做三件事：
把 LeRobot 字段 repack 成训练/推理统一字段
observation.images.top_head
observation.images.hand_left
observation.images.hand_right
observation.state
action

调用 ARXInputs/ARXOutputs 做 ARX 平台专属的数据适配：相机 key 重命名；图像格式转换；state 补零到 32 维；output只取14维度
无论是什么机械臂最终都完全相同的格式进入模型：                  
    │    image["base_0_rgb"]     uint8 [H,W,C] │
    │    image["left_wrist_0_rgb"]             │
    │    image["right_wrist_0_rgb"]            │
    │    state                  float [32]     │
    │    actions                float [50, 32] │
    └─────────────────────────────────────────┘

再走 model transform
prompt 注入
图片 resize 到 224×224
prompt tokenize
states/actions pad


当调用 config.data.create(assets_dirs, model_config) 时，返回一个完整的 DataConfig，包含三层 transform：
Layer 1: repack_transforms（仅训练时，把 parquet 列名映射到统一 key）
  "observation.images.top_head"  → images["top_head"]
  "observation.images.hand_left" → images["hand_left"]
  "observation.images.hand_right"→ images["hand_right"]
  "observation.state"            → state
  "action"                       → actions

Layer 2: data_transforms（训练+推理都用）
  输入: ARXInputs
    - 相机 key 重命名: top_head → base_0_rgb, hand_left → left_wrist_0_rgb, ...
    - state pad 到 32 维
    - actions pad 到 32 维，超出[-π,π]的值置0
    - 图像 CHW→HWC，float→uint8
  输出: ARXOutputs
    - 取 actions[:, :14]（只返回14维给机器人）

Layer 3: model_transforms（训练+推理都用）
  - InjectDefaultPrompt("Fetch and hang the cloth.")
  - ResizeImages(224, 224)
  - TokenizePrompt（PaliGemma tokenizer, max_len=200）
  - PadStatesAndActions（再次确保维度=32）





# 使用自己的数据集微调：
第一步：准备 转换后的LeRobot 数据
第二步：改 config.py--数据集，配置文件以及base checkpoint
第三步：算 norm stats
第四步：开始训练 `uv run --offline scripts/train.py`






# AWBC强化学习后训练流程
Step 0：手工标注 stage_progress_gt
输入：原始 LeRobot 数据集（包含 observation、action 等）
操作：在每个 episode 的 parquet 中新增 stage_progress_gt 列
需要标记任务是否成功，以及确定子任务分界时间点
在每个 stage 内线性插值 0→1，最终值域 [0, 1]
输出：同一数据集，多了 stage_progress_gt 列

| | KAI0                                         | PI06 |
|---|---|---|
| 训练数据 | 都需要 stage_progress_gt（完全一样） | 都需要 stage_progress_gt（完全一样） |
| 训练输入 | 6路图像（当前帧+随机对比帧）           | 3路图像（仅当前帧，无 his_-100_ 图像） |
| 推理方式 | 输入(帧A, 帧B)两帧 → 直接输出相对优势  | 每帧独立预测绝对值 → 再用差值算优势 |



Step 1：训练 Advantage Estimator
输入：Step 0 产出的带 stage_progress_gt 的数据集
配置：ADVANTAGE_TORCH_KAI0_FLATTEN_FOLD（config.py 中定义）
skip_norm_stats=True（不需要归一化统计）
训练机制：AdvantageLerobotDataset 会在同 episode 内随机采样一个对比帧（标记为 his_-100_），计算 progress = stage_progress_gt - his_-100_stage_progress_gt 作为回归目标
命令：uv run --offline scripts/train_pytorch.py ADVANTAGE_TORCH_KAI0_FLATTEN_FOLD --exp-name=run1


Step 2：用训练好的 Estimator 给数据集打优势分数
输入：针对任务的LeRobot 数据集+ Step1 的 checkpoint
脚本：stage_advantage/annotation/eval.py
操作：
遍历每个 episode，读取三路视频帧
批量 GPU 推理，预测每帧的优势值
不修改原始数据，输出到新目录 data_KAI0_100000/，加入了离散优势分数relative_advantage、absolute_value、absolute_advantage
目录结构：
<repo_id>/
  ├── data/                        # 原始数据（不变）
  ├── data_KAI0_100000/            # 新目录，含打分后的parquet
  │     chunk-000/
  │         episode_000000.parquet  # = 原始 + advantage列
  └── videos/                      # 共享（不变）




Step 3：离散化优势值 → AWBC 训练标签
输入：Step 2 产出的带优势分数的 parquet + 原始数据集的 videos 和 meta
脚本：discretize_advantage.sh
操作：
组装标准 LeRobot 目录（link videos, copy meta, copy data_KAI0_100000/ → data/）
按全局百分位阈值（如 top 30%）将连续优势值二值化
task_index就是根据阈值，二值化得到的0-1标签
写入 task_index 列到每个 parquet
同时写入 meta/tasks.jsonl：
task_index=0 → "fold the cloth, Advantage: negative"
task_index=1 → "fold the cloth, Advantage: positive"
输出：一个完整的、可直接用于 AWBC 训练的 LeRobot 数据集目录



Step 4：AWBC 训练
输入：Step 3 产出的 advantage 数据集
配置：pi05_hang_cloth_awbc（config.py 中定义）
repo_id → Step 3 输出的数据集路径
base_config=DataConfig(prompt_from_task=True) → 覆盖原始提示词
每帧先有task_index，如果 prompt_from_task=True，task_index → 查 meta/tasks.jsonl从而进行匹配 → 得到 prompt，例如"Flatten and fold the cloth, Advantage: positive"
weight_loader → pi0.5 base checkpoint
训练前：先跑 compute_norm_states_fast.py --config-name pi05_hang_cloth_awbc 计算归一化统计
训练机制：模型以 "..., Advantage: positive/negative" 为语言条件进行行为克隆，positive 帧的 prompt 引导模型学习高优势行为
推理时：传入 "Fetch and hang the cloth, Advantage: positive" 作为 prompt，引导策略执行高优势动作




# 训练方式
gs://openpi-assets/checkpoints/pi05_base/params 就是 π₀.5 base 权重路径；你本地缓存的那份可以给 Kai0 用，但要把 weight_loader 或 OPENPI_DATA_HOME 配好，并把数据集路径/norm stats 配好。

FSDP 是一种分布式训练策略，核心思想是：将模型参数沿某个维度切片（shard）分散到多张 GPU 上，而不是每张卡都存一份完整副本。需要计算时再临时聚合，计算完后释放，从而大幅降低单卡显存占用。
hang_cloth 用的配置是 pi05_hang_cloth_normal，fsdp_devices 默认为1，即未启用FSDP，走的是纯数据并行。如果你有多卡且显存不够，可以设 fsdp_devices=2 或更大值来启用FSDP，效果是：
显存减半（模型参数被切到2张卡上）
代价：卡间通信增加，训练速度可能略慢
batch_size 可以开更大，因为单卡省下了存参数的显存，batchsize增大，学习率可以略微增大一点。

# 训练数据读取：
随机抽一个时刻 t 帧
→ 取这个时刻的图像、状态、prompt
→ 用从这个时刻开始的未来动作 chunk 作为监督
→ 训练模型学会“看到当前场景后，接下来一小段时间该怎么动”。
监督信号就是 action，而且通常不是单步 action，而是 t 开始的一段 action chunk。

数据集里先按帧存单步 action
训练时抽到某一帧 t
再由 loader 按 action_horizon 把这帧后面的连续 action 拼成一个 chunk
每个 action[t] 都只是 该时间步的一条 14 维动作

按 **kai0 代码里真实发生的顺序** 来看，训练时的数据流大致是：

**`scripts/train.py`**
→ `create_data_loader(...)`
→ 读 LeRobot 数据
→ 一串 transforms 做字段重排、机器人适配、归一化、图像缩放、prompt tokenize、state/action pad
→ batch 化
→ 转成 `Observation, actions`
→ 喂给 `model.compute_loss(...)`。

# 1. 第一步：`TrainConfig` 先把“这份数据该怎么读”定义好

`create_data_loader()` 一开始会先执行：

```python
data_config = config.data.create(config.assets_dirs, config.model)
```

也就是先把你在 `TrainConfig` 里写的 `LerobotARXDataConfig`、`LerobotAgilexDataConfig` 之类，展开成真正的数据配置。然后：

* 如果是 RLDS 路线，就走 `create_rlds_data_loader`
* 否则就走 `create_torch_data_loader`。

对你一直在看的 **ARX / HangCloth / Task_C** 这条线，配置里默认会把 LeRobot 原始字段先 repack 成统一结构：

* `observation.images.top_head`
* `observation.images.hand_left`
* `observation.images.hand_right`
* `observation.state`
* `action`

并指定 `action_sequence_keys=("action",)`；随后再加上 `arx_policy.ARXInputs(...)` 这种机器人专用 transform。([GitHub][3])

---

# 2. 第二步：底层先读的是 LeRobot dataset，不是你自己手写逐帧循环

`create_torch_dataset()` 会直接创建：
* `LeRobotDatasetMetadata(repo_id)`
* `LeRobotDataset(repo_id, delta_timestamps=...)`

这里最关键的是 `delta_timestamps`。kai0 会对 `action` 这个 key 生成：

* `0/fps`
* `1/fps`
* `2/fps`
* ...
* `49/fps`

这样的时间偏移。也就是说，**数据集底层本来只存逐帧 `action`，但 loader 在取到当前帧样本时，会顺着时间轴把未来 `action_horizon` 个动作拼成一段 action sequence**。默认 `Pi0Config` 里 `action_horizon=50`。([GitHub][2])

所以，训练时你可以把“抽到一个样本”理解成：

* 当前时刻的一帧图像
* 当前时刻的一个 state
* 再外加从该时刻开始的 **50 步动作块**

而不是 parquet 里每一帧都提前存了 `[50, 14]`。

---

# 3. 第三步：如果是 AWBC，还会先把 `task_index` 变成 prompt

如果数据配置里 `prompt_from_task=True`，`create_torch_dataset()` 会在 LeRobotDataset 外面再包一层 `PromptFromLeRobotTask(dataset_meta.tasks)`。它的逻辑很直白：

* 读当前样本的 `task_index`
* 去 `tasks` 映射里查
* 返回 `prompt`。([GitHub][2])

所以对 AWBC 来说，样本在这一步就从：

* 图像 / state / actions / `task_index`

变成了：

* 图像 / state / actions / `prompt`。([GitHub][2])

如果不是 AWBC，或者数据里没有 `task_index`，后面就会由 `default_prompt` 兜底。([GitHub][3])

---

# 4. 第四步：真正的训练前处理，靠一串 transforms 顺序执行

`transform_dataset()` 会把 dataset 再包一层 `TransformedDataset`，并且按固定顺序执行四类输入 transform：

1. `repack_transforms.inputs`负责把 LeRobot 原始字段名整理成训练统一字段。
2. `data_transforms.inputs`负责做 **机器人平台适配**
3. `Normalize(...)` 归一化
4. `model_transforms.inputs`。

这一步是 **kai0 数据流最核心的地方**。


# 5. 第五步：再由 DataLoader 做 batch、shuffle、worker 并发

`create_torch_data_loader()` 在拿到 transform 后的 dataset 以后，会创建 `TorchDataLoader`。
里面实际调用的是 `torch.utils.data.DataLoader(...)`，并设置了：

* `batch_size=local_batch_size`
* `shuffle`
* `num_workers`
* `persistent_workers`
* 自定义 `collate_fn`
* `drop_last=True`。([GitHub][2])

`collate_fn` 的作用是把单条样本堆成 batch。然后：

* 如果是 **JAX** 训练，就用 `jax.make_array_from_process_local_data(...)` 把 batch 变成分片数组
* 如果是 **PyTorch** 训练，就转成 torch tensor。([GitHub][2])

最后 `DataLoaderImpl.__iter__()` 会把 batch 包成：

```python
(Observation.from_dict(batch), batch["actions"])
```

也就是说，训练侧真正拿到的是一个二元组：

* `observation`
* `actions`。

---

# 6. 第六步：batch 真正进入模型

`train.py` 里第一次拿 batch 后，会把它打印出来做 sanity check，然后每个 step 都执行：

* `train_state, info = ptrain_step(train_rng, train_state, batch)`

在 `train_step()` 里：

* `observation, actions = batch`
* `chunked_loss = model.compute_loss(rng, observation, actions, train=True)`
* 再对 loss 求均值、反传、更新参数。([GitHub][1])

所以，从模型视角看，它吃到的就是：

## 输入

* 三路图像
* image mask
* state
* tokenized prompt / prompt mask

## 监督

* `actions`，形状是 `[batch, action_horizon, action_dim]`

对 ARX 而言，动作语义是“每步 14 维有效动作，pad 到模型统一维度后再训练”。([GitHub][5])

## 最后用一句最朴素的话总结

**是的，kai0 训练时需要 dataloader。**
而且它不是“直接把 parquet 原样塞进模型”，而是要先经过：

* **读数据**
* **按 horizon 组 action chunk**
* **字段重排**
* **机器人平台适配**
* **归一化**
* **图像缩放**
* **prompt tokenization**
* **state/action pad**
* **batch 化**
最后才进入模型。






# vla训练含义
kai0这样的vla算法，他训练的时候，action和state虽然数值一样，但action是chunk，从而作为监督信号。训练时，随机取时间步和对应的帧，训练机器人在一个随机的时间步下的图片进程下，该做出什么动作。




# 推理流程数据流
一、训练阶段（产出 checkpoint）
数据准备：下载数据集到 ./data，可选做数据增强（时间缩放、空间镜像）
配置：在 src/openpi/training/config.py 中设置 repo_id（数据路径）和 weight_loader（基础 checkpoint 路径）
计算归一化统计：scripts/compute_norm_states_fast.py
训练：scripts/train.py 启动 pi0/pi0.5 全参数微调，checkpoint 写入指定目录
（可选）Model Arithmetic：多个 checkpoint 做加权合并
（可选）Stage Advantage：用优势估计器做 AWBC 训练
最终产物是一个 checkpoint 目录（含 params/ 或 model.safetensors + assets/norm_stats）。


二、推理阶段（两机架构）
服务器端（GPU 主机）：serve_policy.py
create_policy() → create_trained_policy() 加载 checkpoint：
检测是 JAX（params/）还是 PyTorch（model.safetensors）格式
加载模型权重 + 归一化统计
构建 Policy 对象，包含输入/输出 transform 链（归一化、prompt 注入等）
启动 WebsocketPolicyServer，监听 WebSocket 连接，等待客户端请求

推理脚本：机器人端（IPC）：arx_openpi_inference_rtc.py
初始化 ROS2 节点 ARX5ROSController：
推理脚本订阅左右臂 topic（/arm_slave_l_status, /arm_slave_r_status）看作关节状态
与此同时他也会发布master话题，这也是为什么只运行slave节点代码就可以

推理脚本 pub --> /arm_master_l_status --> slave sub (FollowCmdCallback)
slave pub --> /arm_slave_l_status --> 推理脚本 sub (joint_left_callback)

初始化 RealSense 相机
创建关节指令 Publisher
创建 WebsocketClientPolicy 连接到 GPU 服务器

启动推理线程（RTC 或普通模式）：
从 ROS 订阅获取关节位置 qpos（14维）+ 3 个相机图像，图像 BGR→RGB、resize 到 224x224
打包为 payload = {state, images, prompt, ...}
调用 policy.infer(payload) → WebSocket 序列化发送到服务器
服务器处理：收到 obs → input transforms（归一化等）→ model.sample_actions() → output transforms（反归一化）→ 返回 actions（shape: [chunk_size, 14]）
客户端收到 actions，放入 StreamActionBuffer 做时序平滑（temporal smoothing），然后打包为ros话题

主控制循环（同一机器人端进程）
以 control_frequency（30Hz）循环：
stream_buffer.pop_next_action() 逐步弹出单个 action（14维关节位置）
ros_operator.set_joint_positions(act) → 构建 RobotStatus 消息 → 通过 ROS2 Publisher 发布到 /arm_master_l_status 和 /arm_master_r_status topic
重要：使用了原遥操作主臂的master节点，策略推理得到的action动作代替了主臂遥操作的控制，伪装成遥操作
从而订阅master话题消息，发布slave，执行动作。如果同时启动master主臂节点，则主臂会控制从臂导致不动


三、总结
policy.infer(obs) 背后的机制是：
客户端的 policy 是 WebsocketClientPolicy，它将 obs 通过 WebSocket 发送到服务器
服务器的 Policy 对象已经加载了训好的 checkpoint（模型权重+归一化参数）
服务器执行前向推理，返回 action chunk
action 回到机器人端后，经过平滑处理，逐帧通过 set_joint_positions() 发布为 ROS2 的 RobotStatus 消息到对应 topic，驱动真实机械臂执行





# Checkpoint 目录的具体结构
训练产出两种格式的 checkpoint，目录结构如下：

JAX 格式（scripts/train.py 产出）
checkpoints/<config_name>/<exp_name>/<step>/
├── params/                    # Orbax OCDBT 格式，存储模型权重
│   └── (二进制分片文件)        # JAX PyTree，每个参数都是 jax.Array
├── train_state/               # 训练状态（optimizer state、EMA params 等）
│   └── (二进制分片文件)
└── assets/
    └── <asset_id>/
        └── norm_stats.json    # 归一化统计，JSON 格式

PyTorch 格式（scripts/train_pytorch.py 产出）
checkpoints/<config_name>/<exp_name>/<step>/
├── model.safetensors          # safetensors 格式，存储模型所有权重
├── optimizer.pt               # PyTorch 原生格式，optimizer state_dict
├── metadata.pt                # 训练元数据（step、config、timestamp）
└── assets/
    └── <asset_id>/
        └── norm_stats.json

norm_stats.json 示例
{
  "norm_stats": {
    "actions": {
      "mean": [0.01, -0.03, ...],   // 14维（双臂各7自由度）
      "std": [0.12, 0.08, ...],
      "q01": [-0.5, -0.3, ...],     // 1% 分位数
      "q99": [0.5, 0.3, ...]        // 99% 分位数
    },
    "state": {
      "mean": [...],
      "std": [...],
      "q01": [...],
      "q99": [...]
    }
  }
}





# 3d打印-夹爪
.SLDPRT（SolidWorks Part） SolidWorks 专属零件文件  可直接改参数、改尺寸，模型自动更新
.STEP/.STP 导入后不能直接编辑原始特征（只能加新特征）
.3MF 专为 3D 打印设计的现代格式（替代 STL）
File formats: Use STEP for CAD exchange and most slicers; use 3MF when provided for direct 3D printing. SLDPRT / SLDDRW / SLDASM are for SolidWorks only.

夹爪控制中，在 arx_openpi_dagger_collect.py中，夹爪和 6 个手臂关节一起被当作第 7 维，以统一的关节位置指令方式下发；但在推理阶段它被**专门做了"二值化阈值化"**再发布，同时，replay和部署要乘以5




# 环境打包迁移至无网服务器
/mnt/workspace/xiajiawei/kai0_off_env 这套打包方式挺值得复用。它不是简单把 .venv 复制走，而是用 uv 的 lockfile + uv cache + uv-managed Python + 源码 + 恢复脚本 来做“可重建”的离线环境
核心思想是：

在有网机器上先把项目依赖完整解析好：

uv lock
uv sync --frozen
把 uv 下载过的依赖缓存搬走：

/root/.cache/uv  ->  stage/uv_cache
把 uv 自己管理的 Python 搬走：

uv_python/cpython-3.11.11-linux-x86_64-gnu
把 uv 二进制也放进去：

stage/bin/uv
目标无网机器上不直接复用 .venv，而是重新生成：

UV_CACHE_DIR=/path/to/uv_cache \
UV_PYTHON_INSTALL_DIR=/path/to/uv_python \
uv sync --frozen --offline --python 3.11.11
这比直接复制 .venv 稳，因为 .venv 里很多路径、软链接、解释器位置可能和原机器绑定。这里让 uv 在目标路径重新创建 .venv，但所有“材料”都来自本地 cache，不访问网络。

恢复脚本做了什么

restore_kai0_offline.sh 逻辑很清楚：

rsync -a kai0 bin uv_python uv_cache "$TARGET_DIR/"
export UV_CACHE_DIR="$TARGET_DIR/uv_cache"
export UV_PYTHON_INSTALL_DIR="$TARGET_DIR/uv_python"
export UV_LINK_MODE=copy
export UV_NO_PROGRESS=1

cd "$TARGET_DIR/kai0"
"$TARGET_DIR/bin/uv" sync --frozen --offline --python 3.11.11
关键点是：

--frozen
严格使用 uv.lock，不重新解析依赖。

--offline
禁止联网，只用本地 uv_cache。

UV_LINK_MODE=copy
避免硬链接跨文件系统失败，迁移场景更稳。

压缩和校验

