# ARX Control ros2
ARX Control 仓库地址
https://github.com/ARXroboticsX/ARX_X5

机械臂启动与数据采集说明
/home/agilex/ARX_X5-main


# can口检查脚本
# 从臂
# can1-左   和can2-右
# 检查脚本，但注意会杀死进程，但依然要先运行，再运行启动can
'/home/agilex/ARX_X5-main/ARX_X5-main/ARX_CAN/arx_can/arx_can1.sh' 
'/home/agilex/ARX_X5-main/ARX_X5-main/ARX_CAN/arx_can/arx_can2.sh' 


# 主臂
# can0-左  can3-右
# 先运行如下，再运行下列的启动can
'/home/agilex/ARX_X5-main/ARX_X5-main/ARX_CAN/arx_can/arx_can0.sh' 
'/home/agilex/ARX_X5-main/ARX_X5-main/ARX_CAN/arx_can/arx_can3.sh' 



# 机械臂驱动开启与遥操作
# 上述命令可以不用管了，直接运行如下


# 先直接启动can
sudo slcand -o -f -s8 /dev/arxcan1 can1
sudo ifconfig can1 up

sudo slcand -o -f -s8 /dev/arxcan2 can2
sudo ifconfig can2 up

sudo slcand -o -f -s8 /dev/arxcan0 can0
sudo ifconfig can0 up

sudo slcand -o -f -s8 /dev/arxcan3 can3
sudo ifconfig can3 up





# 单个机械臂-左臂：

单臂左遥操作：
终端1-从臂
cd /home/agilex/ARX_X5-main/ARX_X5-main/ROS2/X5_ws
source install/setup.bash
ros2 launch arx_x5_controller open_remote_slave._single.launch.py
上述不但可以遥操作，同样也是机械臂关节控制的驱动


如果使用末端位置姿态控制，
ros2 launch arx_x5_controller open_single_arm.launch.py
但该launch无法被主臂遥操作


终端2-主臂
cd /home/agilex/ARX_X5-main/ARX_X5-main/ROS2/X5_ws
source install/setup.bash
ros2 launch arx_x5_controller open_remote_master_single.launch.py






# 双臂
cd /home/agilex/ARX_X5-main/ARX_X5-main/ROS2/X5_ws
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch arx_x5_controller open_remote_slave.launch.py
ros2 launch arx_x5_controller open_remote_master.launch.py








# 键盘测试机械臂
cd /home/agilex/ARX_X5-main/ARX_X5-main/ROS2/X5_ws
source install/setup.bash
ros2 launch arx_x5_controller open_single_arm.launch.py

改yaml要重新编译
cd /home/agilex/ARX_X5-main/ARX_X5-main/ROS2/X5_ws
source install/setup.bash
ros2 run arx_x5_controller KeyBoard

KeyBoard -> /arm_cmd -> X5Controller normal 模式 -> can1 单臂

常用按键：
w/s      x 方向前后
a/d      y 方向左右
方向键上/下  z 方向上下
n/m      roll
./l      pitch
,/ /     yaw
o/c      夹爪开/合
i        重力补偿
r        回零





若需要重新编译，例如改yaml

cd /home/agilex/ARX_X5-main/ARX_X5-main/ROS2/X5_ws

rm -rf build install log
colcon build
